// Expired seconds of cache data.
pub const BATCHES_CACHE_EXPIRED_SECS: u64 = 1;
pub const BLOCKS_CACHE_EXPIRED_SECS: u64 = 1;
pub const LAST_BATCH_INDEXES_CACHE_EXPIRED_SECS: u64 = 1;

// Query parameter `page` starts from `1`, and default `per_page` is 20.
pub const DEFAULT_PER_PAGE: u64 = 20;
